apiVersion: v1
kind: Service
metadata:
  name: gateway-service
spec:
  type: LoadBalancer
  selector:
    app: gateway-deployment
  ports:
    - protocol: TCP
      port: 8084
      targetPort: gateway-port
  externalIPs:
    - 192.168.49.2
