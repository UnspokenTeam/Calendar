datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-py"
}

model Invite {
  id String @id @default(uuid())
  event_id String
  user_id String
  status InviteStatus
}

enum InviteStatus {
  PENDING
  ACCEPTED
  REJECTED
}
